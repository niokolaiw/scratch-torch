# scratch-torch
Build PyTorch-style neural networks with Scratch blocks. Train real models (TensorFlow.js) in TurboWarp — pandas-style data blocks, torch-style layers, sklearn-style fit/predict — with a live graph of every connection and its learned weights
